# 👥 User Management System

A simple **User Management System** built for practice purposes using **React (frontend)** and a lightweight **Node.js backend (without MongoDB)**.  
It demonstrates how to send data from a frontend form to a backend API and display the data dynamically.

---

## 🚀 Features

- Add new users (name & email)
- Store users on a simple backend server (JSON file or in-memory array)
- Fetch and display user list on the frontend
- Clean and minimal UI
- Practice project – no database used (only fetch API)

---

## 🧰 Tech Stack

### Frontend:
- React
- Vite 
- Fetch API
- CSS

### Backend:
- Node.js
- Express.js
- CORS enabled
- **express.json() middleware** for JSON body parsing
